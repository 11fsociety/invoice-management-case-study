// tsx scripts/seed.ts - populates admin user + default parameters.
export {};
async function main() {
  console.log("seed: TODO");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
